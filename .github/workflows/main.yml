name: Deploy Gracespring foundation

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted 

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: Build Next.js
        run: npm run build

      - name: Sync files & Restart PM2
        run: |
          rsync -avz --delete --exclude='.git' --exclude='node_modules' ./ /var/www/foundation/
          cd /var/www/foundation
          npm install --production
          pm2 restart "foundation" || pm2 start npm --name "foundation" -- run start -- -p 3003
          pm2 save


          




# # This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# # For more information see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages

# name: Node.js Package

# on:
#   push:
#     branches: ["main"]

# jobs:
#   build:
#     runs-on: self-hosted
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#       - run: npm ci
#       - run: npm run build --if-present

# .github/workflows/build.yml
# name: Build

# on: [push, pull_request]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v2

#     - name: Set up Node.js
#       uses: actions/setup-node@v2
#       with:
#         node-version: '20.0.0' # Use the Node.js version specified in your project

#     - name: Install dependencies
#       run: npm install

#     - name: Build the project
#       run: npm run build




# on: 
#   push:
#     branches:
#       - main
# name: 🚀 Deploy website on push
# jobs:
#   web-deploy:
#     name: 🎉 Deploy
#     runs-on: ubuntu-latest
#     steps:
#     - name: 🚚 Get latest code
#       uses: actions/checkout@v4

#     - name: Use Node.js 20
#       uses: actions/setup-node@v2
#       with:
#         node-version: '20.0.0'
      
#     - name: 🔨 Build Project
#       run: |
#         npm install
#         npm run build

#     - name: Deploy via FTP
#       uses: SamKirkland/FTP-Deploy-Action@4.3.0
#       with:
#         server: ${{ secrets.GIT_CPANEL_SERVER }} # Your FTP server
#         username: ${{ secrets.GIT_CPANEL_USER }} # Your FTP username
#         password: ${{ secrets.GIT_CPANEL_PWD }} # Referencing the FTP password stored as a secret
#         protocol: ${{ secrets.GIT_CPANEL_PROTOCOL }}
#         port: ${{ secrets.GIT_CPANEL_PORT }} # FTP port
#         local-dir: ./ # Local directory to deploy (adjust if necessary)
#         server-dir: /heelheidbusiness/ # Adjust based on your server
  
